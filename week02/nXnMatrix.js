function drawMatrix(dimension) {
    for (let i = 0; i < dimension; i++){
        for (let i = 0; i < dimension; i++){
            console.log(`${dimension} `);
        }
        console.log()
    }
}

drawMatrix(3);